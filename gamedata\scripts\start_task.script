function give_start_task()
    local actor = db.actor
    if actor then
        actor:give_task("find_bury_task", actor)
    end
end

-- Хук на спавн актёра
function actor_binder:net_spawn(data)
    self.object:give_task("find_bury_task", self.object)
    return true
end
